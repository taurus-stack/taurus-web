<template>
	<fs-page>
		<fs-crud ref="crudRef" v-bind="crudBinding" />
	</fs-page>
</template>

<script lang="ts" setup name="programHostBinding">
import { ref, onMounted } from 'vue';
import { useFs } from '@fast-crud/fast-crud';
import { createCrudOptions } from './crud';

const crudRef = ref();
const { crudBinding, crudExpose, resetCrudOptions } = useFs({ createCrudOptions });

onMounted(() => {
	crudExpose.doRefresh();
});
</script>